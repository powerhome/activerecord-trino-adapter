name: activerecord-trino-adapter

on:
  push:

jobs:
  ruby:
    uses: powerhome/github-actions-workflows/.github/workflows/ruby-gem.yml@bd38636f54897c992d5b09a86c86707e4b5b7734 # main
    with:
      package: ${{ github.workflow }}
      workdir: .
      gemfiles: "['gemfiles/rails_7_1.gemfile','gemfiles/rails_7_2.gemfile','gemfiles/rails_8_0.gemfile']"
      ruby: '["3.3"]'
    secrets: inherit
